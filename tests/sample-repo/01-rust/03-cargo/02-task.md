---
id          = "cargo_sha2_rust"
name        = "Cargo SHA2"
language    = "rust"
difficulty  = 2
description = "Implement a simple hash hex function"
topics      = ["SHA2", "Cargo"]
---

# Cargo SHA2

Implement the `hex_hash` function so it takes a string slice and returns its SHA-256 hash as a lowercase hex string.

Use the `sha2` crate to compute the digest.

